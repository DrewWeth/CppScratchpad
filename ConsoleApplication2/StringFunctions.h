#pragma once
#include <string>
#include <iterator>

using std::string;

// Join iterator
//string join_str( <string>::iterator iter );
//std::string join(Iterator begin, Iterator end, const std::string& delimiter) {
